---
import Base from '../../../layouts/Base.astro';
import SozlukBody from '../../../components/SozlukBody.astro';
import { SOZLUK_UI, landingTitle, landingDesc, landingJsonLd } from '../../../data/sozluk';
const lang = 'en' as const;
const t = SOZLUK_UI[lang];
---
<Base lang={lang} page="sozluk" title={landingTitle(lang)} description={landingDesc(lang)} ogType="website" jsonld={landingJsonLd(lang)}>
  <SozlukBody lang={lang} />
</Base>
